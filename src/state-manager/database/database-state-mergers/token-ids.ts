import { TOKEN_SYMBOL } from '../../../constant/tokens';

export const tokenIdMaps = new Map([
  [TOKEN_SYMBOL.ETH, 'ethereum'],
  [TOKEN_SYMBOL.BTC, 'bitcoin'],
  [TOKEN_SYMBOL.BNB, 'binancecoin'],
  [TOKEN_SYMBOL.USDT, 'tether'],
  [TOKEN_SYMBOL.USDC, 'usd-coin'],
  [TOKEN_SYMBOL.DAI, 'dai'],
]);
